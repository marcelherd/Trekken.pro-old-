import type { NextPage } from "next";
import { Layout } from "../modules/layout/Layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <h1>Home</h1>
    </Layout>
  );
};

export default Home;
